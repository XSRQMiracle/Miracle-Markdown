[package]
name = "typeset-core"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "TeX-quality line breaking and CJK typesetting engine"

[dependencies]
hypher = "0.1"

[dev-dependencies]

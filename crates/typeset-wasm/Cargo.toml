[package]
name = "typeset-wasm"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
typeset-core = { path = "../typeset-core" }
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
